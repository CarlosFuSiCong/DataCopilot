/** Rows per page for result / diff server pagination. */
export const RESULT_PAGE_SIZE = 50

/** Hide Δ badge when result toolbar is narrower than this (px). */
export const RESULT_DELTA_BADGE_MIN_HEADER_PX = 500

/** Below min(panel body, original|result bar) width: compact dims & short Browse/Back labels. */
export const PANEL_DIMS_COMPACT_BREAKPOINT_PX = 560
