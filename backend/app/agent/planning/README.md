# Planning

This layer owns converting task intent and context into workflow plans.

Current modules:

- `workflow_planner`: existing LLM workflow planner.

Future MVP5 decomposition:

- `intent_parser`
- `parameter_resolver`
- `workflow_builder`
