# Tool Selection

This layer owns the tool registry and future step-level tool selection.

Current modules:

- `registry`: `ToolSpec` definitions, metadata, validation dispatch, and execution dispatch.

Future work:

- Candidate tool generation.
- Policy filtering.
- Rule-based scoring.
- Optional LLM reranking.
