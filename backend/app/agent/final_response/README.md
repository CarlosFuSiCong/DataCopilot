# Final Response

This layer owns grounded explanations and final user-facing reports.

Current modules:

- `result_explainer`: generates explanations from actual execution results.

Final responses should distinguish executed results from previews, warnings, errors, and unverified suggestions.
