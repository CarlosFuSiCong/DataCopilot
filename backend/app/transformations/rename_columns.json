{
  "type": "rename_columns",
  "description": "Rename one or more columns using an old-name to new-name mapping.",
  "keywords": [
    "rename", "alias", "mapping", "column name", "relabel",
    "重命名", "改名", "列名", "字段名", "别名"
  ],
  "parameters": [
    {
      "name": "mapping",
      "type": "dict[str, str]",
      "required": true,
      "description": "A dictionary mapping old column names to new column names."
    }
  ],
  "example": {
    "type": "rename_columns",
    "mapping": {
      "sales": "total_sales",
      "region": "area"
    }
  }
}
